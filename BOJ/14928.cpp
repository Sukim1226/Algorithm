// Å« ¼ö (BIG)
#include <cstdio>
#include <cstring>
#define DIV 20000303

// 0 <= m <= n <= 10^1000
char n[1000005];

unsigned long long divide(int start, int end) {


}

int main(void) {
    int nlen;
    scanf("%s", n);
    
    for (int i = 0; i < nlen; i++) {
        n[i] -= '0';
    }

    nlen = strlen(n);
    for (int i = nlen / 10; i >= 0; i--) {
        
    }

    return 0;
}